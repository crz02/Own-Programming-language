#include <iostream>
using namespace std;

int main() {
    while (true) {
        string text;
        cout << "basic > ";
        getline(cin, text);
        cout << text << endl;
    }
    return 0;
}